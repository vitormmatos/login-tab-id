import { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'login-tab-id',
  webDir: 'www',
  bundledWebRuntime: false
}

export default config
